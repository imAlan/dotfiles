syntax enable
set background=dark
let g:solarized_termcolors=256
colorscheme solarized

set tabstop=4 softtabstop=0 expandtab shiftwidth=4 smarttab

set expandtab           " Change tab to spaces
set shiftwidth=4        " 4 spaces for tab
set tabstop=4
set encoding=utf-8

set number              " Line Numbers
set ruler               " Displays line number, column number, relative percentage
set showmode            " Shows mode
set title               " Show filename in window title
set ignorecase          " Ignore cases when searching
set smartcase           " Search lower case is not case sensitive 
set showcmd             " Show imcomplete commands


"syntax on               "vim syntax highlighting
